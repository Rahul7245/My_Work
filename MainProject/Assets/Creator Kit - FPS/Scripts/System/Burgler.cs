using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Burgler : MonoBehaviour
{
    // Start is called before the first frame update
    public int m_value;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public int getValue() {
        return m_value;
    }
}
